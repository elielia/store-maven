import { Routes } from '@angular/router';
import { TaskScheduleComponent } from '@app/task-schedule/task-schedule.component';

export const taskScheduleRoutes: Routes = [{ path: '', component: TaskScheduleComponent }];
